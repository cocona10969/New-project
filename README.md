# 黄子盈个人作品集 · 基础版

暗色、星际感的 React + Vite 单页作品集。当前项目图片与头像为概念占位视觉，后续可直接替换真实素材。

## 本地运行

```bash
npm install
npm run dev
```

浏览器访问终端显示的本地地址即可。

## 素材替换

- Hero 已预留 `/public/hero-space.webm` 视频路径；没有视频时会自动显示动态星空 Canvas。
- 头像占位位于 `src/App.jsx` 的 `portrait` 模块。
- 项目内容集中在 `src/App.jsx` 顶部的 `projects` 数据中。
